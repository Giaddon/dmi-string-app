import styled from 'styled-components';

// CSS for the alert box div.
export default styled.div`
  background-color: skyblue;
  margin-top: 15px;
  color: #fff;
  border-radius: 4px;
  padding: 0.1em 1em;
  pointer-events: none;
`;
