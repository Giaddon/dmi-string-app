import styled from 'styled-components';

// Styled h1 for Navbar, used for app title.
export default styled.h1`
  display: inline-block;
  position: relative;
  top: 50%;
  transform: translateY(-50%);
  color: #fff;
  font-size: 1.5rem;
  font-familiy: sans-serif;
  margin: 0 0 0 10px;
  padding: 0;
`;
